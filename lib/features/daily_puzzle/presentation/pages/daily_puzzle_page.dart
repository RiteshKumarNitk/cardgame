import 'package:flutter/material.dart';

/// Placeholder for the Daily Puzzle feature — real daily challenge content
/// and rewards are added in a later phase.
class DailyPuzzlePage extends StatelessWidget {
  const DailyPuzzlePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Daily Puzzle')),
      body: const Center(child: Text('Daily Puzzle (placeholder)')),
    );
  }
}
